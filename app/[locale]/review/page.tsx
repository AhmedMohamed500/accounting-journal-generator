import { ReviewCenter } from "@/components/accounting/review-center";
import type { Locale } from "@/types";
export default async function Page({ params }: { params: Promise<{ locale: Locale }> }) { const { locale } = await params; return <div className="container section"><h1>{locale === "ar" ? "مركز مراجعة واعتماد القيود" : "Entry Review Center"}</h1><p className="muted">{locale === "ar" ? "تابع القيد من المسودة حتى المراجعة والاعتماد والترحيل مع سجل كامل للحركات." : "Move entries from draft through review, approval, and posting with a complete audit trail."}</p><ReviewCenter locale={locale} /></div>; }
