# تحديث أداة تحليل Excel المحاسبي

تاريخ التحديث: 13 أغسطس 2026

## الهدف

تطوير قسم تحليل Excel في FINORA ليعمل كأداة مالية ومحاسبية متكاملة، وليس مجرد عارض إحصائي للبيانات. أصبح النظام يقرأ صفوف ملفات `XLSX` و`CSV`، يكتشف أعمدتها، يصنف الحركات، ويقترح قيدًا يوميًا متوازنًا لكل حركة قبل إرسالها إلى دورة القيود الموحدة.

## ما تم تنفيذه

- تحديث اسم الصفحة إلى **محلل Excel المالي والمحاسبي**.
- اكتشاف تلقائي لأعمدة التاريخ والبيان والمبلغ والمدين والدائن والتصنيف وكود الحساب والحساب المقابل والمرجع والطرف.
- إمكانية تعديل ربط الأعمدة يدويًا إذا كانت أسماء أعمدة الملف غير معتادة.
- تصنيف الحركات إلى:
  - الإيرادات.
  - المصروفات الإدارية والعمومية.
  - المصروفات والعمولات البنكية.
  - الرواتب والأجور.
  - الإيجارات.
  - الكهرباء والمياه والمرافق.
  - الصيانة والإصلاح.
  - التسويق والإعلان.
  - الضرائب.
  - القروض والتمويل.
  - رأس المال.
  - الأصول والمعدات.
  - المخزون والمشتريات وتكلفة المبيعات.
  - التحويلات بين الحسابات.
  - بند يحتاج تصنيفًا يدويًا عند عدم كفاية البيانات.
- عرض إجمالي التدفقات الداخلة والخارجة وصافي الحركة وعدد الحركات المصنفة والحركات التي تحتاج مراجعة.
- عرض ملخص حسب التصنيف مع عدد الحركات وإجمالي كل فئة.
- جدول مراجعة احترافي يسمح بتعديل التاريخ والبيان والاتجاه والتصنيف وحساب العملية والحساب المقابل.
- إظهار القيد المقترح مدينًا ودائنًا لكل صف مع نسبة ثقة وتنبيهات المراجعة.
- ترقيم النتائج وعرض 25 حركة في الصفحة للحفاظ على ثبات الواجهة مع الملفات الكبيرة.
- تحديد حتى 500 حركة في الدفعة الواحدة لإنشاء القيود دون تجميد المتصفح.
- إنشاء القيود كـ **مسودات فقط** داخل Journal Engine الموحد؛ لا يتم الاعتماد أو الترحيل تلقائيًا.
- إضافة مصدر جديد للقيود باسم **تحليل Excel المحاسبي / Accounting Excel Analysis**.
- منع إنشاء قيود للحركات غير المصنفة.
- منع استخدام الحساب نفسه في طرفي القيد، خصوصًا في التحويلات التي تحتاج تحديد حسابين مختلفين.
- الاعتماد على دليل الحسابات المركزي والحسابات القابلة للترحيل فقط.

## مسار العمل الجديد

رفع ملف Excel أو CSV
→ اكتشاف الأعمدة
→ تصنيف الحركات
→ مراجعة الحسابات والاتجاه
→ إنشاء قيود متوازنة كمسودات
→ مركز القيود
→ المراجعة والاعتماد
→ الترحيل
→ الأستاذ والتقارير المالية.

## الملفات الجديدة

- `lib/spreadsheet/accounting-intelligence.ts`
- `components/spreadsheet/accounting-intelligence.tsx`
- `tests/spreadsheet-accounting-intelligence.test.ts`
- `EXCEL_ACCOUNTING_INTELLIGENCE_UPDATE_AR.md`

## الملفات المعدلة

- `app/[locale]/spreadsheet-analysis/page.tsx`
- `components/spreadsheet/spreadsheet-analyzer.tsx`
- `lib/accounting/journal.ts`
- `types/accounting.ts`
- `types/spreadsheet-analysis.ts`

## التحقق والجودة

- `npm run typecheck`: ناجح.
- `npm run lint`: ناجح بدون تحذيرات.
- `npm run test`: ناجح — 47 ملف اختبار و232 اختبارًا.
- `npm run build`: ناجح — تم إنشاء نسخة الإنتاج وجميع صفحات المشروع.
- تجربة متصفح فعلية بملف عربي: نجحت قراءة وتصنيف الإيرادات والمصروفات الإدارية والعمولات البنكية والرواتب، ثم إنشاء 4 قيود متوازنة كمسودات وظهورها في مركز القيود.

## ملاحظات الاستخدام

- يجب مراجعة الحساب المقابل قبل إنشاء المسودات؛ الافتراضي للحركات البنكية هو حساب البنوك.
- التحويل بين حسابين يتطلب اختيار حسابين مختلفين.
- الصفوف غير الواضحة لا يتم تخمينها أو ترحيلها، بل تظهر تحت **تحتاج تصنيف**.
- البيانات والتحليل والقيود ما زالت تعتمد على طبقة التخزين المحلية الحالية، بدون Backend أو Database أو API مدفوع.

---

## إصلاح القراءة وإنشاء صفر قيود — 13 أغسطس 2026

### المشكلة

كانت بعض الملفات تُقرأ في التحليل العام، لكن أداة القيود تعرض **صفر قيود** لأن عناوين الأعمدة لم تكن مطابقة للقائمة المحدودة، أو لأن المبالغ مكتوبة بأرقام عربية وعلامات عملة، أو لأن البيان موزع على أكثر من عمود. وكان الصف الذي لا يملك مبلغًا صالحًا يُحذف بالكامل من شاشة المراجعة.

### الإصلاحات

- توسيع قاموس عناوين التاريخ والبيان والمبلغ والمدين والدائن بالعربية والإنجليزية.
- توحيد الحروف العربية عند المقارنة، مثل: `أ/إ/آ` و`ى/ي` و`ة/ه`، وإزالة التشكيل وعلامات الاتجاه المخفية.
- إضافة اكتشاف ذكي يعتمد على **محتوى العمود** عندما تكون العناوين مثل `A` و`B` و`C` أو أسماء غير معروفة.
- دعم المبالغ المكتوبة:
  - بالأرقام العربية `١٢٣٤` والفارسية.
  - بالفاصلة العربية `٬` والعلامة العشرية `٫`.
  - مع عملات مثل `ج.م` و`EGP` و`SAR`.
  - بين أقواس كقيمة سالبة.
- تجميع البيان من عمود الوصف والفرع والملاحظات والبند بدل أخذ أول نص فقط.
- عدم إسقاط أي صف يحتوي على بيان حتى لو كان المبلغ ناقصًا؛ يظهر الصف في جدول المراجعة مع تنبيه واضح.
- إضافة حقل مبلغ قابل للتعديل داخل كل صف قبل إنشاء القيد.
- اختيار الحركات الصالحة تلقائيًا بدل الاعتماد على نسبة ثقة مرتفعة فقط.
- السماح بإنشاء مسودة للحركة غير المصنفة على حساب التسوية المؤقت `1199` مع تحذير مراجعة، بدل أن يؤدي التصنيف غير المعروف إلى تعطيل الدفعة كلها.
- إضافة مؤشرات توضح:
  - عدد الصفوف المستخرجة.
  - عدد البيانات التي تم استكمالها من تفاصيل الصف.
  - عدد المبالغ التي تحتاج إدخالًا.
- دعم رفع ملفات `XLSM` بجانب `XLSX` و`CSV`، مع استمرار رفض صيغة `XLS` القديمة غير الآمنة.
- تحسين الأداء بحساب خصائص الأعمدة مرة واحدة لكل ورقة بدل تكرار التحليل لكل صف.

### التحقق الإضافي

- أضيفت اختبارات للعناوين غير المعتادة والاستنتاج من محتوى الأعمدة.
- أضيفت اختبارات للأرقام والعملات العربية والبيانات الموزعة والمبالغ الناقصة.
- تم اختبار ملف عربي فعلي يحتوي على صف عنوان إضافي، 6 أعمدة، 5 حركات، مبلغ مفقود، وحركة غير مصنفة.
- استخرج النظام الحركات الخمس، استُكمل المبلغ الناقص من الواجهة، ثم تم إنشاء **5 قيود متوازنة** وظهرت في مركز القيود بمصدر **تحليل Excel المحاسبي**.
- `npm run typecheck`: ناجح.
- `npm run lint`: ناجح بدون تحذيرات.
- `npm run test`: ناجح — 47 ملف اختبار و238 اختبارًا.
- `npm run build`: ناجح — تم إنشاء نسخة الإنتاج بكل الصفحات.
