import{FinancialReports}from"@/components/accounting/financial-reports";import type{Locale}from"@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"دفتر الأستاذ والقوائم المالية":"Ledger & Financial Statements"}</h1><p className="muted">{locale==="ar"?"تقارير مبنية على القيود المتوازنة والمرحّلة فقط.":"Reports use balanced, posted entries only."}</p><FinancialReports locale={locale}/></div>}
