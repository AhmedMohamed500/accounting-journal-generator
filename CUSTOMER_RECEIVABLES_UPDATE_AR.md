# FINORA — تحديث مركز حسابات العملاء الآجلين

تاريخ التحديث: 18 أغسطس 2026

## النتيجة

تم إنشاء مركز مستقل لحسابات العملاء الآجلين داخل كل شركة، مع الاعتماد على نفس بيانات الأطراف ومحرك القيود ودليل الحسابات وطبقة التخزين الموجودة في FINORA. لا توجد قاعدة بيانات أو API خارجي أو تكلفة تشغيل جديدة.

رابط الشاشة داخل النظام:

- العربية: `/ar/receivables`
- الإنجليزية: `/en/receivables`

## دورة العمل

عميل داخل الشركة النشطة
→ فاتورة بيع آجل
→ رصيد مفتوح وتاريخ استحقاق
→ مسودة قيد مرتبطة بالفاتورة
→ مراجعة واعتماد وترحيل من مركز القيود
→ تحصيل جزئي أو كامل
→ مسودة قيد تحصيل مرتبطة بالفاتورة
→ تحديث كشف الحساب وأعمار الديون والرصيد المتبقي.

## بيانات العميل

- كود عميل فريد وحساب أستاذ مساعد.
- الاسم بالعربية والإنجليزية.
- الرقم الضريبي والسجل التجاري.
- مسؤول التواصل والهاتف وواتساب والبريد والعنوان.
- أيام الائتمان وحد الائتمان.
- حالة نشط أو موقوف.
- فصل البيانات باستخدام الشركة النشطة في نظام مساحات العمل الحالي.

## فاتورة البيع الآجل

تدعم الشاشة:

- العميل ورقم الفاتورة وتاريخها وتاريخ الاستحقاق.
- وصف الشغل أو التوريد.
- الصافي ونسبة ضريبة القيمة المضافة.
- اختيار حساب الإيراد من دليل الحسابات المركزي.
- منع تكرار رقم الفاتورة لنفس العميل.
- منع الفاتورة لعميل موقوف أو بتاريخ استحقاق غير صحيح.
- تنبيه عند تجاوز حد الائتمان.

القيد الناتج يُحفظ كمسودة من مصدر `Customer Receivables`:

- مدين: حساب العملاء `1120` بإجمالي الفاتورة.
- دائن: حساب الإيراد المختار بالصافي.
- دائن: ضريبة القيمة المضافة — مخرجات `2201` إن وجدت.

## التحصيل

- اختيار العميل ثم الفاتورة المفتوحة.
- تحصيل جزئي أو كامل.
- اختيار حساب الخزينة أو البنك من دليل الحسابات.
- منع تحصيل قيمة أكبر من المتبقي.
- حفظ مرجع التحصيل وربطه بالفاتورة والقيد.

قيد التحصيل:

- مدين: الخزينة أو البنك المختار.
- دائن: العملاء `1120`.

## الرقابة والتقارير

- إجمالي العملاء النشطين.
- إجمالي المستحق والمتأخر.
- عدد الفواتير المفتوحة.
- الائتمان المتاح.
- أعمار الدين: غير مستحق، 1–30، 31–60، 61–90، وأكثر من 90 يومًا.
- مؤشر حالة العميل وتجاوز حد الائتمان.
- كشف حساب متحرك يعرض الفواتير والتحصيلات والرصيد بعد كل حركة.
- قائمة بكل القيود المرتبطة بالعميل وحالتها.
- روابط مباشرة إلى مركز القيود وملف العميل 360°.

## التكامل مع الشاشات القديمة

- تم تحديث شاشة العملاء والموردين الحالية بحيث تنشئ فاتورة العميل مسودة قيد بدل إنشاء رصيد منفصل بدون قيد.
- تم تحديث التحصيل من شاشة الأطراف وملف 360° لاستخدام نفس محرك حسابات العملاء.
- تم الاحتفاظ بالتوافق مع الفواتير والتحصيلات القديمة، ويظهر الرصيد المسدد السابق داخل كشف الحساب.
- لم يتم إنشاء نموذج قيود موازٍ؛ كل الحركات تستخدم `saveEntry` وJournal Engine الحالي.

## أهم الملفات

### ملفات جديدة

- `app/[locale]/receivables/page.tsx`
- `components/parties/customer-receivables-center.tsx`
- `lib/parties/receivables.ts`
- `tests/customer-receivables.test.ts`

### ملفات معدلة

- `components/layout/header.tsx`
- `components/parties/party-center.tsx`
- `components/parties/party-profile-360.tsx`
- `lib/accounting/journal.ts`
- `types/accounting.ts`
- `types/parties.ts`

## الاختبارات والفحص

- `typecheck`: ناجح.
- `lint`: ناجح بدون تحذيرات.
- `test`: 49 ملف اختبار و245 اختبارًا ناجحًا.
- `build`: ناجح، وتم توليد مسار `/[locale]/receivables` ضمن نسخة الإنتاج.
- اختبار متصفح فعلي: فاتورة آجلة بإجمالي 11,400 ثم تحصيل 4,000، وأصبح المتبقي 7,400 مع ظهور القيدين في كشف العميل.

## حدود المرحلة الحالية

البيانات محفوظة داخل المتصفح ومفصولة لكل شركة محليًا. الاستخدام الحقيقي المتزامن من أجهزة مختلفة يحتاج Backend وقاعدة بيانات وصلاحيات مركزية ومزامنة، وهي غير مضافة في هذه المرحلة.
