
import {Generator} from "@/components/generator/generator";import type {Locale} from "@/types";export const metadata={title:"Journal Entry Generator | FINORA"};export default async function Page({params,searchParams}:{params:Promise<{locale:Locale}>;searchParams:Promise<{type?:string}>}){const [{locale},q]=await Promise.all([params,searchParams]);return <div className="container section"><h1>{locale==="ar"?"مولّد القيود اليومية":"Journal Entry Generator"}</h1><p className="muted">{locale==="ar"?"اكتب العملية بلغتك أو استخدم النموذج الموجّه. تعمل القواعد محليًا دون API مدفوع.":"Describe the transaction or use the guided form. Rules run locally without a paid API."}</p><Generator locale={locale} initialType={q.type}/></div>}
