import{DocumentCycle}from"@/components/documents/document-cycle";import type{Locale}from"@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"الدورة المستندية":"Document Cycle"}</h1><p className="muted">{locale==="ar"?"أنشئ فواتير البيع والشراء وسندات القبض والصرف، ثم راجعها واعتمدها لإنشاء القيود تلقائيًا.":"Create sales, purchase, receipt, and payment documents, then approve them to generate accounting entries."}</p><DocumentCycle locale={locale}/></div>}
