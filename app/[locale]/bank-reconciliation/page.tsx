import{BankReconciliation}from"@/components/banking/bank-reconciliation";import type{Locale}from"@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"التسوية البنكية الذكية":"Smart Bank Reconciliation"}</h1><p className="muted">{locale==="ar"?"استورد كشف البنك، طابق الحركات تلقائيًا، وأنشئ مسودات قيود للحركات الناقصة.":"Import bank statements, auto-match transactions, and draft entries for missing activity."}</p><BankReconciliation locale={locale}/></div>}
