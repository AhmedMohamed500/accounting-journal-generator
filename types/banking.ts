import type{GeneratedJournalEntry}from"./accounting";export interface BankTransaction{id:string;date:string;description:string;reference:string;debit:number;credit:number;balance?:number;currency:string;matchedEntryId?:string;matchConfidence?:number;status:"unmatched"|"matched"|"ignored"}export interface BankMatch{transactionId:string;entryId:string;confidence:number;reasons:string[]}export interface ReconciliationSummary{total:number;matched:number;unmatched:number;ignored:number;matchRate:number;statementDebits:number;statementCredits:number}export interface BankMatchInput{transactions:BankTransaction[];entries:GeneratedJournalEntry[];toleranceDays?:number}
