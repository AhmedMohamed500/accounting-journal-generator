import {SmartCloseCenter} from "@/components/closing/smart-close-center";import type {Locale} from "@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"مركز الإقفال الذكي":"Smart Close Center"}</h1><p className="muted">{locale==="ar"?"حوّل إقفال الشهر من متابعة يدوية مشتتة إلى خطة موحدة تقيس الجاهزية والوقت والمخاطر.":"Turn month-end close into one measurable plan for readiness, time, and risk."}</p><SmartCloseCenter locale={locale}/></div>}
