import{InvoiceCapture}from"@/components/invoice/invoice-capture";import type{Locale}from"@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"قراءة الفاتورة واقتراح القيد":"Invoice Capture & Entry Proposal"}</h1><p className="muted">{locale==="ar"?"ارفع فاتورة، راجع البيانات المستخرجة، ثم أنشئ مسودة قيد تمر بدورة الاعتماد.":"Upload an invoice, review extracted data, then create a draft entry for approval."}</p><InvoiceCapture locale={locale}/></div>}
