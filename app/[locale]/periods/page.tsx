import{PeriodManager}from"@/components/periods/period-manager";import type{Locale}from"@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"الفترات والسنوات المالية":"Fiscal Years & Periods"}</h1><p className="muted">{locale==="ar"?"تحكم في فتح وإقفال الفترات واحمِ البيانات المرحّلة من التعديل.":"Control period status and protect posted accounting data from changes."}</p><PeriodManager locale={locale}/></div>}
