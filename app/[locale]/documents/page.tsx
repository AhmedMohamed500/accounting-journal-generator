import{DocumentInbox}from"@/components/documents/document-inbox";import type{Locale}from"@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"صندوق المستندات المحاسبي":"Accounting Document Inbox"}</h1><p className="muted">{locale==="ar"?"اجمع المستندات، امنع التكرار، تابع المراجعة واربط كل مستند بقيده ومهمته.":"Collect documents, block duplicates, track review, and link each file to its entry and task."}</p><DocumentInbox locale={locale}/></div>}
