import type{AccountingDocument}from"@/types";import{loadOperationalData,saveOperationalData}from"./accounting";const KEY="accounting-documents";export function loadDocuments():AccountingDocument[]{return loadOperationalData<AccountingDocument[]>(KEY,[])}export function saveDocuments(items:AccountingDocument[]){saveOperationalData(KEY,items)}export async function fingerprintFile(file:File){const bytes=await file.arrayBuffer();const hash=await crypto.subtle.digest("SHA-256",bytes);return[...new Uint8Array(hash)].map(v=>v.toString(16).padStart(2,"0")).join("")}
