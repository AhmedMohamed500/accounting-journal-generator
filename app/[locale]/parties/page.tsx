import{PartyCenter}from"@/components/parties/party-center";import type{Locale}from"@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const{locale}=await params;return <div className="container section"><h1>{locale==="ar"?"حسابات الموردين والمستحقات":"Supplier Accounts & Payables"}</h1><p className="muted">{locale==="ar"?"سجّل الموردين وفواتير الشراء الآجلة والسداد وأعمار المستحقات من مكان واحد.":"Manage suppliers, credit purchase invoices, payments, and payable aging in one place."}</p><PartyCenter locale={locale}/></div>}
