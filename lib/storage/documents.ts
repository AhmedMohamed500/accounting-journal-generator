import type{AccountingDocument}from"@/types";import{companyKey}from"./accounting";const KEY="accounting-documents";export function loadDocuments():AccountingDocument[]{if(typeof window==="undefined")return[];try{return JSON.parse(localStorage.getItem(companyKey(KEY))||"[]")as AccountingDocument[]}catch{return[]}}export function saveDocuments(items:AccountingDocument[]){localStorage.setItem(companyKey(KEY),JSON.stringify(items))}export async function fingerprintFile(file:File){const bytes=await file.arrayBuffer();const hash=await crypto.subtle.digest("SHA-256",bytes);return[...new Uint8Array(hash)].map(v=>v.toString(16).padStart(2,"0")).join("")}
