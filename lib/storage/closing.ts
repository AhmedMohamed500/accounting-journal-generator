import {defaultCloseChecklist} from "@/data/closing"; import {companyKey} from "./accounting"; import type {CloseChecklistItem} from "@/types";
const KEY="smart-close-checklist"; export function loadCloseChecklist(period:string){if(typeof window==="undefined")return defaultCloseChecklist;try{return JSON.parse(localStorage.getItem(`${companyKey(KEY)}:${period}`)||"null") as CloseChecklistItem[]||defaultCloseChecklist.map(i=>({...i}))}catch{return defaultCloseChecklist.map(i=>({...i}))}} export function saveCloseChecklist(period:string,items:CloseChecklistItem[]){localStorage.setItem(`${companyKey(KEY)}:${period}`,JSON.stringify(items))}
