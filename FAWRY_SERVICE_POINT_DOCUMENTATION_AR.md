# توثيق نظام فوري ونقاط الخدمات في FINORA

## Zero-Cost Demo Edition

أضيفت طبقة عرض وبيع محلية كاملة حول محرك نقطة الخدمات الحالي من دون تغيير منطقه المالي. يستطيع العميل بدء نموذج جاهز أو إعداد نشاط جديد عبر Onboarding من سبع خطوات، ثم تجربة المستخدمين المحليين والصلاحيات وPIN وسجل النشاط والنسخ الاحتياطي والتجربة والباقات والترقية اليدوية وجولة المنتج.

الواجهة Responsive على الهاتف والتابلت والكمبيوتر، ولوحة المالك تعرض الربح والخزنة وأرصدة فوري وكل المحافظ والعمليات والتنبيهات مع إمكانية إخفاء القيم. صفحة الباقات موجودة على `/ar/service-point/plans` و`/en/service-point/plans` وتقرأ الأسعار والحدود من `data/service-point-plans.ts`.

> **تنبيه المزامنة:** كل جهاز أو Browser يحتفظ ببيانات Service Point مستقلة داخل `localStorage`. فتح رابط Vercel على الهاتف يعطي تجربة Responsive كاملة، لكنه لا ينقل بيانات الكمبيوتر إلى الهاتف. المزامنة، الدخول الآمن، النسخ التلقائي، وسجل التدقيق المركزي هي خصائص Cloud Edition مستقبلية ولم تُنفذ في هذه النسخة.

> **تنبيه الأمان:** أدوار المستخدمين وPIN والصلاحيات وسجل النشاط والتفعيل كلها محاكاة محلية مناسبة للعرض وجهاز واحد. PIN لا يُحفظ كنص صريح، لكنه لا يمثل بديلًا عن Backend Authentication. لا توجد بوابة دفع أو عملية تحصيل وهمية.

## 1. نظرة عامة

يوفر المشروع وحدة متكاملة لإدارة محل فوري أو نقطة خدمات مالية من داخل FINORA. الوحدة لا تتعامل مع فوري كزر دفع فقط، بل تدير دورة العمل اليومية كاملة: فتح الوردية، تسجيل العمليات، متابعة الخزنة وأرصدة الخدمات، حساب العمولة والتكلفة والربح، إنشاء القيود المحاسبية، مطابقة الأرصدة عند الإقفال، وإصدار الإيصالات والتقارير.

المسار الرئيسي للوحدة:

- العربية: `/ar/service-point`
- الإنجليزية: `/en/service-point`
- لوحة صاحب المحل: `/ar/service-point/owner-dashboard`

> **نطاق النسخة الحالية:** النظام هو طبقة تشغيل ومحاسبة محلية داخل المتصفح. لا يوجد حاليًا اتصال مباشر بواجهات Fawry أو المحافظ، ولا تنفيذ تلقائي للتحويلات، ولا مزامنة حقيقية بين الأجهزة. هذه الوظائف تحتاج Backend وتكاملًا رسميًا مع مقدم الخدمة.

## 2. الخدمات المدعومة

كل مقدم خدمة له رصيد مستقل وحساب أستاذ مستقل:

| الخدمة | المعرّف البرمجي | كود الحساب |
|---|---|---:|
| فوري | `fawry` | `1181` |
| فودافون كاش | `vodafone-cash` | `1182` |
| أورنج كاش | `orange-cash` | `1183` |
| إي آند كاش | `etisalat-cash` | `1184` |
| أمان | `aman` | `1185` |
| مصاري | `masary` | `1186` |
| إنستا باي | `instapay` | `1187` |

وجود حساب منفصل لكل خدمة يمنع خلط رصيد فوري برصيد أي محفظة أخرى، ويجعل المطابقة والربحية أكثر دقة.

## 3. دورة التشغيل اليومية

### 3.1 إنشاء المحل واختياره

- يمكن إنشاء أكثر من محل داخل نفس الشركة.
- لكل محل خزنة وورديات وعمليات وقيود وتقارير مستقلة.
- يمكن رفع شعار بصيغة PNG أو JPG أو WebP حتى 1.5 ميجابايت.
- يظهر شعار المحل في الإيصالات والتقارير المطبوعة.

### 3.2 فتح الوردية

يُدخل المستخدم:

- اسم الكاشير.
- رصيد الخزنة الفعلي في بداية الوردية.
- الرصيد الفعلي لكل ماكينة أو محفظة، ومنها رصيد فوري.

لا يسمح النظام بفتح أكثر من وردية واحدة في الوقت نفسه للمحل نفسه.

### 3.3 تسجيل العمليات

العمليات المتاحة هي:

| نوع العملية | تأثير الخزنة | تأثير رصيد الخدمة | الربح |
|---|---|---|---|
| تحويل للعميل | يزيد بالقيمة وعمولة العميل | ينخفض بالقيمة وتكلفة مقدم الخدمة | العمولة ناقص التكلفة |
| سحب نقدي للعميل | ينخفض بصافي المبلغ المدفوع | يزيد بصافي قيمة العملية | العمولة ناقص التكلفة |
| دفع فاتورة | يزيد بالقيمة وعمولة العميل | ينخفض بالقيمة والتكلفة | العمولة ناقص التكلفة |
| شحن رصيد | يزيد بالقيمة وعمولة العميل | ينخفض بالقيمة والتكلفة | العمولة ناقص التكلفة |
| تغذية رصيد الخدمة | ينخفض بالقيمة | يزيد بالقيمة | صفر |
| تحويل بين أرصدة المحل | لا يتغير | ينخفض المصدر ويزيد المستلم | صفر |
| مصروف محل | ينخفض بالقيمة | لا يتغير | مصروف بالقيمة |

بيانات العملية تشمل نوعها، حالتها، الخدمة، القيمة، عمولة العميل، تكلفة مقدم الخدمة، والمرجع أو رقم الهاتف. قبل الحفظ تظهر معاينة لتأثير العملية على الخزنة ورصيد الخدمة وإيراد العمولة وصافي الربح.

### 3.4 حالات المعاملة

- `successful`: عملية ناجحة؛ تتحرك الأرصدة ويُنشأ القيد المحاسبي ويُرحّل فورًا.
- `pending`: عملية معلقة؛ تُحفظ للمتابعة من دون تحريك الأرصدة أو إنشاء قيد.
- `failed`: عملية فاشلة؛ تُحفظ من دون أثر مالي أو محاسبي.
- `reversed`: عملية ناجحة جرى استردادها بقيد وحركة عكسيين.

يمكن اعتماد العملية المعلقة لاحقًا فتنشأ حركتها المحاسبية، أو تعليمها كعملية فاشلة من دون أثر على الأرصدة.

### 3.5 إقفال الوردية

عند نهاية اليوم:

1. يجهز النظام الأرصدة المتوقعة للخزنة ولكل خدمة.
2. يدخل الكاشير الأرصدة الفعلية بعد العد والمراجعة.
3. يحسب النظام فرق كل رصيد وإجمالي العجز أو الزيادة.
4. ينشئ قيد تسوية متوازن إذا وُجد فرق.
5. تُغلق الوردية مع حفظ وقت الإقفال والأرصدة الفعلية.

## 4. المنطق المالي

### 4.1 حساب الربح الحقيقي

```text
صافي ربح العملية = عمولة العميل - تكلفة مقدم الخدمة

الربح الحقيقي للفترة = إجمالي عمولات العملاء
                         - إجمالي تكاليف مقدمي الخدمة
                         - مصروفات تشغيل المحل
```

قيمة التحويل أو الفاتورة نفسها ليست إيرادًا للمحل. كذلك، التحويل الداخلي بين أرصدة المحل لا يُعد إيرادًا أو مصروفًا أو ربحًا.

### 4.2 الرصيد المتوقع

```text
الخزنة المتوقعة = رصيد أول الوردية + مجموع حركات الخزنة الفعالة

رصيد الخدمة المتوقع = رصيدها أول الوردية
                       + حركاتها المباشرة
                       + التحويلات الداخلية الواردة إليها

فرق الجرد = الرصيد الفعلي - الرصيد المتوقع
```

العمليات المعلقة والفاشلة لا تدخل في حساب الأرصدة أو الربح.

## 5. القيود المحاسبية

### 5.1 دليل الحسابات الخاص بنقطة الخدمات

| الكود | الحساب |
|---:|---|
| `1180` | خزينة نقطة الخدمات |
| `1181` إلى `1187` | أرصدة فوري ومقدمي الخدمات والمحافظ |
| `4400` | إيراد عمولات نقاط الخدمات |
| `5650` | تكلفة وعمولات مقدمي الخدمات |
| `5660` | مصروفات تشغيل نقطة الخدمات |
| `5710` | عجز وزيادة خزينة نقطة الخدمات |

### 5.2 مثال: عملية فوري ناجحة

إذا دفع العميل 1,000 جنيه لتنفيذ خدمة فوري، وكانت عمولة العميل 10 جنيهات وتكلفة فوري 2 جنيه:

```text
من حـ/ خزينة نقطة الخدمات                 1,010
من حـ/ تكلفة وعمولات مقدمي الخدمات           2
    إلى حـ/ رصيد فوري                              1,002
    إلى حـ/ إيراد عمولات نقاط الخدمات                 10
```

- زيادة الخزنة: 1,010 جنيه.
- انخفاض رصيد فوري: 1,002 جنيه.
- صافي الربح: 8 جنيهات.
- إجمالي المدين والدائن: 1,012 جنيه.

### 5.3 الاسترداد

لا يحذف النظام العملية الناجحة. عند الاسترداد:

- يحتفظ بالعملية والقيد الأصليين كسجل تدقيق.
- يغيّر حالة العملية الأصلية إلى `reversed`.
- ينشئ حركة عكسية مرتبطة بالعملية الأصلية.
- ينشئ قيدًا جديدًا بعكس المدين والدائن.

وبذلك تعود الخزنة ورصيد الخدمة والربح إلى وضعها السابق من دون فقد أثر العملية.

## 6. ضوابط الحماية والرقابة

تتضمن الوحدة القواعد التالية:

- منع تنفيذ عملية بقيمة صفر أو قيمة سالبة.
- منع العمولة أو التكلفة السالبة.
- منع التسجيل دون تحديد مقدم الخدمة عندما تتطلب العملية ذلك.
- منع اختيار الرصيد نفسه كمصدر ووجهة للتحويل الداخلي.
- منع العملية إذا أصبحت الخزنة سالبة.
- منع العملية إذا أصبح رصيد فوري أو مقدم الخدمة سالبًا.
- كشف العملية المكررة بالقيمة والمسار والمرجع خلال عشر دقائق.
- كشف العملية المشابهة بلا مرجع خلال دقيقة.
- عدم احتساب العمليات المعلقة أو الفاشلة ضمن الأرصدة الحية.
- تنبيه عند وجود عملية بلا مرجع.
- تنبيه عند تكرار المرجع.
- تنبيه عند استمرار عملية معلقة أكثر من ساعة.
- تنبيه عند وجود عملية مرتبطة بوردية غير معروفة.
- تنبيه عند وجود ورديتين مفتوحتين للكاشير نفسه.

إشارات الرقابة هي تنبيهات للمراجعة وليست اتهامًا تلقائيًا للكاشير. حالات مراجعة التنبيه داخل الواجهة بصرية ومؤقتة في النسخة الحالية، وتحتاج Backend لإنشاء سجل تدقيق مركزي دائم.

## 7. مركز القيادة والتحليلات

تعرض لوحة التشغيل بيانات محسوبة من العمليات الفعلية:

- السيولة الكلية: الخزنة مع مجموع أرصدة الخدمات.
- إجمالي الأموال الداخلة والخارجة.
- عمولات العملاء وتكاليف مقدمي الخدمة.
- الربح الحقيقي بعد مصروفات التشغيل.
- عدد العمليات المعلقة والفاشلة والمستردة.
- مخاطر تكرار العمليات.
- فروق الجرد غير المحسومة.
- حجم وربح وهامش كل مقدم خدمة.
- معدل دوران الرصيد.
- تصنيف الرصيد إلى مناسب أو منخفض أو راكد.
- توصيات لإعادة توزيع السيولة أو تغذية الخدمة، من دون تنفيذ أي تحويل تلقائي.

كما تعرض شاشة الرقابة أداء كل كاشير، بما في ذلك عدد العمليات، حجم الحركة، الإيراد، التكلفة، صافي الربح، متوسط ربح العملية، ساعات العمل، الربح لكل ساعة، وفرق الخزنة.

## 8. التقارير والإيصالات

يمكن تصفية التقارير حسب:

- الفترة: يومي، أسبوعي، أو شهري.
- مقدم الخدمة: فوري وحده، خدمة أخرى، أو إجمالي كل الخدمات.

يتضمن التقرير القابل للطباعة:

- بيانات المحل وشعاره.
- عدد العمليات وحجمها.
- إجمالي العمولات والتكاليف وصافي الربح.
- الأرصدة الحالية.
- تفاصيل كل معاملة وحالتها ومرجعها.
- إمكانية الطباعة أو الحفظ PDF من المتصفح.

ويمكن إصدار إيصال مستقل لكل عملية يحتوي على اسم المحل، الشعار، التاريخ، المرجع، الكاشير، رقم الإيصال، نوع العملية، مقدم الخدمة، القيمة، العمولة، والحالة. الإيصال صادر من نظام إدارة المحل وليس تأكيدًا بنكيًا أو إشعارًا رسميًا من فوري.

مكتبة التحليلات تعرف 22 تقريرًا تشغيليًا ورقابيًا، تشمل العمليات والورديات والخزنة والمحافظ والعمولات والربحية والفروق والسيولة وأداء الكاشير ومقارنة الفروع.

## 9. لوحة صاحب المحل

المسار `/ar/service-point/owner-dashboard` يقدم عرضًا مبسطًا مناسبًا للهاتف ويعرض:

- حالة الوردية والكاشير الحالي.
- رصيد الخزنة والمحافظ.
- ربح اليوم وعدد العمليات.
- العمليات المعلقة والفاشلة.
- التجار النشطين وتنبيهاتهم.
- زر إخفاء القيم المالية للخصوصية.

اللوحة تقرأ البيانات المحلية من الجهاز نفسه حاليًا؛ لا تتزامن بعد بين هاتف صاحب المحل وجهاز الكاشير.

## 10. التخزين وعزل بيانات المحلات

يستخدم النظام `localStorage` مع مفاتيح منفصلة لكل شركة ولكل محل. تُحفظ لكل محل:

- الورديات، حتى 500 وردية.
- العمليات، حتى 5,000 عملية.
- القيود المحاسبية، حتى 5,000 قيد.
- الشعار بصيغة Data URL.

قيود نقطة الخدمات مستقلة عن اليومية والقوائم المالية للشركة الأساسية، ولا تختلط بيانات محل ببيانات محل آخر. توجد أيضًا آلية لترحيل بيانات النسخة القديمة إلى محل افتراضي مستقل عند أول تشغيل بعد التحديث.

> التخزين المحلي مناسب للعرض التجريبي والعمل على جهاز واحد، لكنه ليس بديلًا عن قاعدة بيانات آمنة، صلاحيات مستخدمين، نسخ احتياطي مركزي، وسجل تدقيق على الخادم.

## 11. الملفات الرئيسية في التنفيذ

| الملف | المسؤولية |
|---|---|
| `app/[locale]/service-point/page.tsx` | صفحة نقطة الخدمات الرئيسية |
| `app/[locale]/service-point/owner-dashboard/page.tsx` | صفحة صاحب المحل |
| `components/pos/service-point-center.tsx` | دورة الوردية والعمليات والإقفال والتقارير والإيصالات |
| `components/pos/service-point-intelligence.tsx` | مركز القيادة والربحية والسيولة والتقارير |
| `components/pos/service-point-control.tsx` | رادار الرقابة وتحليل أداء الكاشير |
| `components/pos/owner-dashboard.tsx` | واجهة صاحب المحل المتوافقة مع الهاتف |
| `data/pos.ts` | مقدمو الخدمات وأنواع العمليات ودليل الحسابات |
| `types/pos.ts` | نماذج بيانات المحلات والورديات والعمليات والأرصدة |
| `lib/pos/engine.ts` | الحسابات المالية والقيود والمطابقة والاسترداد |
| `lib/pos/analytics.ts` | مؤشرات الأداء والسيولة ومكتبة التقارير |
| `lib/pos/control.ts` | إشارات الرقابة وأداء الكاشير |
| `lib/storage/pos.ts` | التخزين المحلي وعزل كل محل وترحيل البيانات القديمة |
| `tests/pos.test.ts` | اختبارات العمليات والقيود والأرصدة والاسترداد |
| `tests/pos-control.test.ts` | اختبارات الرقابة وأداء الكاشير |
| `tests/pos-analytics.test.ts` | اختبارات مركز القيادة والتحليلات |
| `tests/pos-store-isolation.test.ts` | اختبار عزل بيانات المحلات |

## 12. الاختبارات المغطاة

تغطي الاختبارات الحالية السيناريوهات الأساسية التالية:

- صحة حساب تحويل العميل وربحه الحقيقي.
- صحة حساب السحب النقدي وعدم خلط الخزنة برصيد المحفظة.
- حساب الأرصدة المتوقعة وفروق الإقفال.
- إنشاء قيد تسوية متوازن للعجز أو الزيادة.
- التحويل بين خدمتين من دون أثر على الخزنة أو الربح.
- دعم إنستا باي كرصيد مستقل.
- استبعاد العمليات المعلقة والفاشلة من الأرصدة.
- كشف التكرار وإنشاء استرداد وقيد عكسي متوازن.
- حساب ربحية الكاشير وفرق الخزنة.
- مؤشرات الربحية والسيولة ومخاطر التكرار.
- عزل عمليات وقيود كل محل عن المحلات الأخرى.

يمكن تشغيل اختبارات الوحدة بالأمر:

```bash
npm test
```

## 13. ما يلزم للنسخة الإنتاجية

لتحويل الوحدة من نسخة محلية إلى منتج إنتاجي متصل فعليًا بفوري، يلزم تنفيذ ما يلي:

1. Backend وقاعدة بيانات مركزية مع نسخ احتياطي.
2. تسجيل دخول وصلاحيات منفصلة للمالك والمدير والكاشير والمراجع.
3. تكامل رسمي مع Fawry APIs بعد التعاقد والحصول على بيانات الاعتماد.
4. Webhooks أو آلية تحقق لتحديث حالة العملية من مقدم الخدمة.
5. تشفير الأسرار وعدم حفظ مفاتيح التكامل في الواجهة أو `localStorage`.
6. سجل تدقيق دائم لكل إنشاء أو تعديل أو اعتماد أو استرداد.
7. موافقة مدير للعمليات الكبيرة والاستردادات الحساسة.
8. مزامنة آمنة بين الأجهزة ومعالجة تعارضات الاتصال.
9. تصدير Excel وPDF من الخادم عند الحاجة إلى تقارير رسمية موحدة.
10. مراقبة الأعطال، التنبيهات، وحدود كل مقدم خدمة اليومية والشهرية.

## 14. الخلاصة

تنفيذ فوري في FINORA هو نظام تشغيل ومحاسبة لنقطة خدمات متعددة الشركات. أهم ما يميزه هو الفصل بين قيمة العملية وإيراد المحل، والفصل بين الخزنة ورصيد كل خدمة، وإنشاء قيد متوازن لكل حركة ناجحة، مع دعم الورديات والمطابقة والاسترداد والرقابة والتقارير. البنية الحالية مناسبة للتجربة المحلية وإثبات الفكرة، بينما يحتاج التشغيل التجاري المتصل بفوري إلى Backend وتكامل رسمي وصلاحيات وأمان على مستوى الإنتاج.
