import {TransactionLibrary} from "@/components/transactions/library";import type {Locale} from "@/types";export default async function Page({params}:{params:Promise<{locale:Locale}>}){const {locale}=await params;return <div className="container section"><h1>{locale==="ar"?"مكتبة العمليات المحاسبية":"Transaction Library"}</h1><p className="muted">{locale==="ar"?"ابحث وفلتر العمليات ثم افتح المولّد بالقواعد المناسبة.":"Search and filter transactions, then open the generator with the right rule."}</p><TransactionLibrary locale={locale}/></div>}
