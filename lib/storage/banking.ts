import type{BankTransaction}from"@/types";import{companyKey}from"@/lib/storage/accounting";const KEY="bank-reconciliation";export function loadBankTransactions(period:string):BankTransaction[]{if(typeof window==="undefined")return[];try{return JSON.parse(localStorage.getItem(`${companyKey(KEY)}:${period}`)||"[]")as BankTransaction[]}catch{return[]}}export function saveBankTransactions(period:string,items:BankTransaction[]){localStorage.setItem(`${companyKey(KEY)}:${period}`,JSON.stringify(items))}
